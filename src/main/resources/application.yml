spring:
  application:
    name: doChallenge-backend

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:doChallenge-local-development-secret-key-change-me}
    access-token-expiration-seconds: ${JWT_ACCESS_TOKEN_EXPIRATION_SECONDS:3600}
